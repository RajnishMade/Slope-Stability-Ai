# Backend deps. Uses TabPFN's HOSTED inference (tabpfn-client) rather than the
# local torch model, so the container stays small and never OOMs on a free/small
# host. Auth is via the TABPFN_TOKEN env var, which tabpfn-client reads
# automatically. No torch, no local model weights.
pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
tabpfn-client==0.3.1
fastapi==0.128.0
uvicorn[standard]==0.39.0
pydantic==2.12.5
